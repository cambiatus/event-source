import { AbstractActionReader } from "demux-js";
import { MongoBlock } from "./MongoBlock";
/**
 * Implementation of an ActionReader that polls a mongodb.
 */
export declare class MongoActionReader extends AbstractActionReader {
    protected mongoEndpoint: string;
    startAtBlock: number;
    protected onlyIrreversible: boolean;
    protected maxHistoryLength: number;
    dbName: string;
    protected mongodb: any;
    protected isInitialized: boolean;
    constructor(mongoEndpoint?: string, startAtBlock?: number, onlyIrreversible?: boolean, maxHistoryLength?: number, dbName?: string);
    initialize(): Promise<void>;
    getHeadBlockNumber(): Promise<number>;
    getBlock(blockNumber: number): Promise<MongoBlock>;
    private throwIfNotInitialized;
}
