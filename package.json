{
  "name": "event-source",
  "version": "1.7.1",
  "description": "Event broker for Cambiatus smart contracts",
  "main": "src/app.js",
  "scripts": {
    "start": "standard ./src/ && node src/app.js",
    "format": "standard ./src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julien Lucca",
  "license": "MIT",
  "dependencies": {
    "@sentry/node": "^4.5.3",
    "demux": "^2.0.0",
    "demux-eos": "^2.0.0",
    "demux-postgres": "^2.0.0",
    "massive": "^6.6.0",
    "pg-native": "^3.0.0"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
